{% extends 'base-admin.html.twig' %}


{% block title %}
    Edit Livre
{% endblock %}

{% block body %}
    <a href="{{ path('admin_livre_index') }}" class="btn btn-dark my-2">
        back to list
    </a>
    <h1>
        Edit Livre
    </h1>

    {{ include('admin_livre/_form.html.twig', {'button_label': 'Update'}) }}

    {{ include('admin_livre/_delete_form.html.twig') }}
{% endblock %}
